export type Genres = {
  id: number;
  image_background: string;
  name: string;
}

export type Plataforms = {
  plataform: {
    id: number;
    name: string;
  }
}

export type Developers = {
  id: number;
  name: string;
}
